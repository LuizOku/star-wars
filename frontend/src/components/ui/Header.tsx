import { FC } from 'react';

const Header: FC = () => (
  <header className="w-full flex justify-center py-6 bg-white shadow-sm">
    <h1 className="text-2xl font-bold font-montserrat text-green-teal tracking-normal">
      SWStarter
    </h1>
  </header>
);

export default Header;
